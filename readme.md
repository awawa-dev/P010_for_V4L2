# DKMS P010 installer

```
wget https://raw.githubusercontent.com/awawa-dev/P010_for_V4L2/refs/heads/master/dkms-installer.sh
sudo chmod +x ./dkms-installer.sh
sudo ./dkms-installer.sh # OR sudo ./dkms-installer.sh 1 for RPi OR sudo ./dkms-installer.sh 2 for Debian/Ubuntu
```